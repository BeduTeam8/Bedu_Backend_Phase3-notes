const os = require("os"); // import os module
let misCpu = os.cpus(); // get cpu info
console.log(misCpu); // print cpu info

let moment = require("moment"); // import moment module
let now = moment(); // get current date
console.log(now); // print current date
